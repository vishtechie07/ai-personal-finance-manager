spring:
  application:
    name: personal-finance-manager

  profiles:
    default: local

server:
  port: 8080
  servlet:
    context-path: /api
logging:
  level:
    com.finance.manager: INFO
    org.springframework: WARN
    org.hibernate: WARN
